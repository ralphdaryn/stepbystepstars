import './Cancel.scss'; // Import the SCSS styles

const Cancel = () => (
  <div className="cancel-container">
    <div className="cancel-content">
      <h1>Payment Canceled</h1>
      <p>You have canceled the payment. Please try again.</p>
    </div>
  </div>
);

export default Cancel;
